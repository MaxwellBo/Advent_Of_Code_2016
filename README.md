### Haskell

#### Setup

- `cabal sandbox init`
- `cabal install containers split mtl parallel cryptohash puremd5`

#### Execution

- `cabal repl`
- `> :l <DAY>`
- `> main`

### Scala

- `sbt`
- `> run <DAY>`
